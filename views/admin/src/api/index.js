const host = "http://localhost:3001";

export const roleService = host+'/roles';

export const userService = host+'/users';

export const orderService = host +'/order';

export const priceService = host +'/price';